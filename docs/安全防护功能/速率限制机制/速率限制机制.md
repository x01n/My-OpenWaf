# 速率限制机制

<cite>
**本文引用的文件**
- [ratelimit.go](file://internal/waf/ratelimit/ratelimit.go)
- [redis.go](file://internal/waf/ratelimit/redis.go)
- [ratelimit_test.go](file://internal/waf/ratelimit/ratelimit_test.go)
- [engine.go](file://internal/core/engine/engine.go)
- [phases.go](file://internal/core/rules/phases.go)
- [protection.go](file://internal/store/protection.go)
- [server.go](file://internal/app/server.go)
- [redis.go](file://internal/core/redis/redis.go)
- [metrics.go](file://internal/dataplane/metrics.go)
- [page.tsx](file://frontend/app/(dashboard)/cc-protection/page.tsx)
- [drop.go](file://internal/waf/drop/drop.go)
- [handler_drop.go](file://internal/admin/handler_drop.go)
- [drop_event.go](file://internal/store/repository/drop_event.go)
- [router.go](file://internal/admin/router.go)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [阻断策略集成](#阻断策略集成)
7. [依赖关系分析](#依赖关系分析)
8. [性能考量](#性能考量)
9. [故障排查指南](#故障排查指南)
10. [结论](#结论)
11. [附录](#附录)

## 简介
本文件系统性梳理 My-OpenWaf 的速率限制机制，覆盖以下要点：
- 速率限制算法实现：固定窗口（本地）、滑动窗口（Redis 分布式）
- Redis 集成与分布式一致性：键空间设计、Lua 原子脚本、失败开策略
- 限流策略配置项：窗口、配额、动作类型、是否启用
- 多维度限流：IP 级、站点级（Host）组合键
- 阻断策略集成：自动阻断阈值配置与统计信息展示
- 性能优化与监控：避免热点、内存清理、指标采集
- 缓存与持久化：Redis KV、配置同步与热重载

## 项目结构
与速率限制直接相关的模块分布如下：
- 内核层（规则执行链）：engine 负责组装阶段，包含请求速率限制阶段
- 规则阶段：按顺序执行 IP 黑白名单、ACL、机器人检测、速率限制等
- 本地限流器：固定窗口计数器，带过期清理
- Redis 限流器：基于有序集合的滑动窗口，Lua 原子脚本
- 阻断执行器：TCP 连接终止，无 HTTP 响应
- 配置模型：保护策略中包含请求/错误速率限制的开关、窗口、配额、动作
- 应用启动：从快照加载保护配置，初始化本地限流器，并支持热重载
- 前端界面：可视化展示与编辑请求/错误速率限制策略

```mermaid
graph TB
subgraph "应用层"
APP["应用启动<br/>server.go"]
FE["前端页面<br/>cc-protection/page.tsx"]
DROP_FE["阻断策略页面<br/>drop-policy/page.tsx"]
end
subgraph "核心引擎"
ENG["引擎<br/>engine.go"]
PIPE["流水线<br/>pipeline.go"]
PHASE["规则阶段<br/>phases.go"]
end
subgraph "限流器"
RL_LOCAL["本地限流器<br/>ratelimit.go"]
RL_REDIS["Redis 限流器<br/>redis.go"]
END
subgraph "阻断系统"
DROP_EXEC["阻断执行器<br/>drop.go"]
DROP_HANDLER["阻断处理器<br/>handler_drop.go"]
DROP_REPO["阻断仓库<br/>drop_event.go"]
BLOCK_PAGE["拦截页面<br/>block.go"]
end
subgraph "配置与存储"
CFG["保护配置模型<br/>protection.go"]
REDIS_OPT["Redis 客户端工厂<br/>redis.go"]
PUBSUB["配置同步 Pub/Sub<br/>pubsub.go"]
KV["Redis KV 缓存<br/>redis_kv.go"]
end
subgraph "监控"
METRICS["数据面指标<br/>metrics.go"]
end
APP --> ENG
APP --> CFG
APP --> REDIS_OPT
APP --> PUBSUB
ENG --> PIPE
PIPE --> PHASE
PHASE --> RL_LOCAL
PHASE --> RL_REDIS
RL_REDIS --> KV
DROP_HANDLER --> DROP_EXEC
DROP_EXEC --> DROP_REPO
DROP_FE --> DROP_HANDLER
APP --> METRICS
FE --> CFG
```

**图表来源**
- [server.go:90-125](file://internal/app/server.go#L90-L125)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)
- [phases.go:103-128](file://internal/core/rules/phases.go#L103-L128)
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)
- [redis.go:12-20](file://internal/waf/ratelimit/redis.go#L12-L20)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [handler_drop.go:31-98](file://internal/admin/handler_drop.go#L31-L98)
- [drop_event.go:17-77](file://internal/store/repository/drop_event.go#L17-L77)
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)
- [redis.go:17-30](file://internal/core/redis/redis.go#L17-L30)
- [pubsub.go:13-31](file://internal/core/redis/pubsub.go#L13-L31)
- [redis_kv.go:13-21](file://internal/cache/redis_kv.go#L13-L21)
- [metrics.go:9-28](file://internal/dataplane/metrics.go#L9-L28)

**章节来源**
- [server.go:90-125](file://internal/app/server.go#L90-L125)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)
- [phases.go:103-128](file://internal/core/rules/phases.go#L103-L128)
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)

## 核心组件
- 本地固定窗口限流器：以"客户端 IP + Host"为键，维护每个窗口内的计数与过期时间，周期性清理过期键，适合单节点部署
- Redis 滑动窗口限流器：以有序集合记录每次允许的时间戳，Lua 脚本原子地清理过期项、计数并写入新项，适合多节点共享状态
- 阻断执行器：立即关闭 TCP 连接，不发送任何 HTTP 响应，记录阻断统计信息
- 引擎阶段：在规则执行链中插入请求速率限制阶段，根据保护配置决定动作类型
- 配置模型：定义请求/错误速率限制的开关、窗口、配额、动作类型等
- 应用启动与热重载：从快照加载保护配置，初始化限流器；通过 Redis Pub/Sub 实现跨节点同步

**章节来源**
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)
- [redis.go:12-20](file://internal/waf/ratelimit/redis.go#L12-L20)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)
- [server.go:220-242](file://internal/app/server.go#L220-L242)

## 架构总览
速率限制在请求处理流程中的位置如下：

```mermaid
sequenceDiagram
participant Client as "客户端"
participant DP as "数据平面监听器"
participant Eng as "引擎"
participant Pipe as "流水线"
participant Phase as "请求速率限制阶段"
participant RL as "本地/Redis 限流器"
Client->>DP : "HTTP 请求"
DP->>Eng : "封装 RequestCtx 并交由引擎"
Eng->>Pipe : "构建阶段链"
Pipe->>Phase : "执行 IP/黑白名单/Acl/Bot"
Phase->>Phase : "匹配后进入速率限制阶段"
Phase->>RL : "Allow(key)"
alt "允许"
RL-->>Phase : "true"
Phase-->>Pipe : "Pass()"
else "拒绝"
RL-->>Phase : "false"
Phase-->>Pipe : "返回拦截动作"
end
Pipe-->>Eng : "RunResult(Action)"
Eng-->>DP : "响应"
DP-->>Client : "HTTP 响应"
```

**图表来源**
- [engine.go:57-129](file://internal/core/engine/engine.go#L57-L129)
- [phases.go:109-127](file://internal/core/rules/phases.go#L109-L127)
- [ratelimit.go:48-62](file://internal/waf/ratelimit/ratelimit.go#L48-L62)
- [redis.go:67-85](file://internal/waf/ratelimit/redis.go#L67-L85)

## 详细组件分析

### 本地固定窗口限流器（Fixed Window）
- 键空间设计：以"客户端 IP + '|' + Host"为键，区分同一 IP 下不同站点的独立配额
- 计数与过期：每个键维护一个原子计数器与到期时间；到期或不存在时重建窗口
- 清理策略：后台定时器定期扫描并删除已过期窗口，防止内存泄漏
- 功能接口：Allow/Increment/IsOverLimit/Reconfigure/Enabled/Close

```mermaid
classDiagram
class RateLimiter {
- mu Mutex
- windows map~string,*window~
- windowS int64
- maxReqs int64
- enabled Bool
- stopCh chan struct
+ Enabled() bool
+ SetEnabled(v bool)
+ Reconfigure(windowSec, maxReqs, enabled)
+ Allow(key string) bool
+ Increment(key string) int64
+ IsOverLimit(key string) bool
+ Close()
- cleaner()
}
class window {
+ count Int64
+ expiry int64
}
RateLimiter --> window : "管理多个窗口"
```

**图表来源**
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)

**章节来源**
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)
- [ratelimit.go:48-62](file://internal/waf/ratelimit/ratelimit.go#L48-L62)
- [ratelimit.go:98-116](file://internal/waf/ratelimit/ratelimit.go#L98-L116)

### Redis 滑动窗口限流器（Sliding Window）
- 键空间设计：以"前缀:rl:键"命名，前缀可区分环境/集群，避免冲突
- 原子操作：Lua 脚本一次性完成清理过期项、计数、写入新项与设置过期时间
- 失败开策略：Redis 调用异常时返回允许，确保服务可用性
- 动作类型：通过上下文控制超限时的动作（如拦截）

```mermaid
flowchart TD
Start(["进入 Allow(key)"]) --> CheckEnabled["检查是否启用"]
CheckEnabled --> |未启用| Allow["返回允许"]
CheckEnabled --> |启用| BuildKey["拼接 Redis Key"]
BuildKey --> Now["获取当前毫秒时间戳"]
Now --> Script["执行 Lua 脚本"]
Script --> ExecOK{"执行成功?"}
ExecOK --> |否| FailOpen["失败开: 允许请求"]
ExecOK --> |是| Compare["比较计数与阈值"]
Compare --> |<=| Allow
Compare --> |>| Deny["返回拒绝"]
Allow --> End(["结束"])
Deny --> End
FailOpen --> End
```

**图表来源**
- [redis.go:67-85](file://internal/waf/ratelimit/redis.go#L67-L85)
- [redis.go:49-64](file://internal/waf/ratelimit/redis.go#L49-L64)

**章节来源**
- [redis.go:12-20](file://internal/waf/ratelimit/redis.go#L12-L20)
- [redis.go:49-64](file://internal/waf/ratelimit/redis.go#L49-L64)
- [redis.go:67-85](file://internal/waf/ratelimit/redis.go#L67-L85)

### 速率限制阶段与引擎集成
- 阶段名称：rate_limit
- 执行逻辑：构造键（客户端 IP + '|' + Host），调用限流器 Allow，若超限则返回对应动作
- 动作类型：由保护配置决定（如拦截）

```mermaid
sequenceDiagram
participant PH as "请求速率限制阶段"
participant Ctx as "RequestCtx"
participant RL as "限流器"
PH->>Ctx : "读取 ClientIP/Host"
PH->>PH : "构造 key = ClientIP + '|' + Host"
PH->>RL : "Allow(key)"
alt "允许"
RL-->>PH : "true"
PH-->>PH : "返回 Pass()"
else "拒绝"
RL-->>PH : "false"
PH-->>PH : "返回动作(含 phase=rate_limit)"
end
```

**图表来源**
- [phases.go:109-127](file://internal/core/rules/phases.go#L109-L127)

**章节来源**
- [phases.go:103-128](file://internal/core/rules/phases.go#L103-L128)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)

### 配置与热重载
- 配置项：请求/错误速率限制的开关、窗口、最大请求数、动作类型
- 默认值：请求窗口 60 秒、配额 300；错误窗口 300 秒、配额 30
- 初始化：应用启动时从快照加载保护配置，创建本地限流器实例
- 热重载：通过 Redis Pub/Sub 发布/订阅，跨节点同步配置变更并动态更新限流器参数

```mermaid
sequenceDiagram
participant Admin as "管理员"
participant App as "应用"
participant PubSub as "Redis Pub/Sub"
participant Nodes as "其他节点"
Admin->>App : "修改保护配置"
App->>PubSub : "Publish reload"
PubSub-->>Nodes : "广播 reload"
Nodes->>Nodes : "Subscribe 回调"
Nodes->>Nodes : "Reload 快照并 Reconfigure 限流器"
```

**图表来源**
- [server.go:220-242](file://internal/app/server.go#L220-L242)
- [pubsub.go:33-67](file://internal/core/redis/pubsub.go#L33-L67)

**章节来源**
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)
- [server.go:90-102](file://internal/app/server.go#L90-L102)
- [server.go:220-242](file://internal/app/server.go#L220-L242)
- [pubsub.go:13-31](file://internal/core/redis/pubsub.go#L13-L31)

### 多维度限流实现
- IP 级：键为客户端 IP
- 站点级：键为"IP + '|' + Host"，实现按站点隔离
- API 级：可在上游规则中扩展键的组成（例如加入路径/方法），但默认阶段仅使用 IP+Host 组合键

**章节来源**
- [phases.go:113-117](file://internal/core/rules/phases.go#L113-L117)
- [ratelimit.go:48-62](file://internal/waf/ratelimit/ratelimit.go#L48-L62)

## 阻断策略集成

### 自动阻断阈值配置
系统支持基于速率限制的自动阻断策略，通过以下配置实现：

- **阻断策略开关**：全局启用/禁用阻断功能
- **Bot 分数阈值**：当 Bot 评分超过设定阈值时触发阻断
- **CVE 自动阻断**：支持 Critical 和 High 级别 CVE 规则的自动阻断
- **阻断来源分类**：支持按 Bot、CVE、规则、IP 信誉等来源进行阻断

```mermaid
flowchart TD
Start(["请求到达"]) --> CheckRate["检查速率限制"]
CheckRate --> |正常| Normal["继续处理"]
CheckRate --> |超限| CheckDrop["检查阻断策略"]
CheckDrop --> |满足阈值| Drop["执行阻断"]
CheckDrop --> |不满足| Block["返回拦截动作"]
Drop --> Record["记录阻断事件"]
Block --> Response["返回拦截响应"]
Normal --> Response
Record --> End(["结束"])
Response --> End
```

**图表来源**
- [drop.go:59-83](file://internal/waf/drop/drop.go#L59-L83)
- [handler_drop.go:57-98](file://internal/admin/handler_drop.go#L57-L98)

### 阻断执行器实现
阻断执行器提供以下核心功能：

- **立即连接终止**：关闭 TCP 连接，不发送任何 HTTP 响应
- **统计信息记录**：原子计数器记录各类阻断来源的统计
- **事件持久化**：将阻断事件存储到数据库，支持查询和分析
- **日志记录**：详细的阻断日志，包含源 IP、规则 ID、详细原因等

```mermaid
classDiagram
class DropExecutor {
- stats *DropStats
- enabled bool
- log *slog.Logger
+ Enabled() bool
+ SetEnabled(v bool)
+ Execute(conn net.Conn, reason DropReason) error
+ GetStats() DropStats
+ ResetStats()
}
class DropStats {
- TotalDropped atomic.Int64
- DroppedByBot atomic.Int64
- DroppedByCVE atomic.Int64
- DroppedByRule atomic.Int64
- LastDropTime atomic.Value
}
class DropReason {
- Source string
- RuleID string
- ClientIP string
- Host string
- Timestamp time.Time
}
DropExecutor --> DropStats : "使用"
DropExecutor --> DropReason : "处理"
```

**图表来源**
- [drop.go:10-17](file://internal/waf/drop/drop.go#L10-L17)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [drop.go:26-35](file://internal/waf/drop/drop.go#L26-L35)

**章节来源**
- [drop.go:10-17](file://internal/waf/drop/drop.go#L10-L17)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [drop.go:59-83](file://internal/waf/drop/drop.go#L59-L83)
- [drop.go:85-100](file://internal/waf/drop/drop.go#L85-L100)
- [drop.go:102-123](file://internal/waf/drop/drop.go#L102-L123)

### 阻断统计与监控
系统提供完整的阻断统计和监控功能：

- **实时统计**：总阻断数、按来源分类的阻断统计
- **24小时历史**：阻断事件的 24 小时统计摘要
- **事件查询**：支持按 IP、来源、时间范围查询阻断事件
- **仪表板集成**：阻断统计集成到系统仪表板

```mermaid
sequenceDiagram
participant User as "用户"
participant API as "阻断API"
participant Repo as "阻断仓库"
participant DB as "数据库"
User->>API : "获取阻断统计"
API->>Repo : "Stats24h()"
Repo->>DB : "查询24小时统计"
DB-->>Repo : "返回统计数据"
Repo-->>API : "聚合统计结果"
API-->>User : "返回统计信息"
```

**图表来源**
- [handler_drop.go:100-109](file://internal/admin/handler_drop.go#L100-L109)
- [drop_event.go:65-77](file://internal/store/repository/drop_event.go#L65-L77)

**章节来源**
- [handler_drop.go:100-109](file://internal/admin/handler_drop.go#L100-L109)
- [drop_event.go:56-77](file://internal/store/repository/drop_event.go#L56-L77)

### 前端阻断策略管理
提供完整的阻断策略配置界面：

- **策略配置**：全局开关、Bot 分数阈值、CVE 自动阻断设置
- **统计卡片**：实时显示各类阻断统计
- **事件列表**：阻断事件的详细列表和分页
- **过滤查询**：支持按 IP、来源、时间范围过滤阻断事件

**章节来源**
- [page.tsx:136-190](file://frontend/app/(dashboard)/drop-policy/page.tsx#L136-L190)
- [page.tsx:192-205](file://frontend/app/(dashboard)/drop-policy/page.tsx#L192-L205)
- [page.tsx:207-294](file://frontend/app/(dashboard)/drop-policy/page.tsx#L207-L294)

### 阻断策略与速率限制的集成点
阻断策略与速率限制机制的集成体现在：

- **自动阻断阈值**：当速率限制超限时，可根据阻断策略配置决定是否执行阻断
- **统计信息展示**：阻断统计与速率限制统计相结合，提供更全面的安全态势
- **统一管理界面**：通过阻断策略页面统一管理所有阻断相关配置

**章节来源**
- [router.go:131-135](file://internal/admin/router.go#L131-L135)
- [router.go:199-200](file://internal/admin/router.go#L199-L200)

## 依赖关系分析
- 本地限流器依赖标准库的并发原语与时间包
- Redis 限流器依赖 go-redis 客户端与 Lua 脚本
- 阻断执行器依赖网络连接管理和原子计数器
- 引擎通过规则阶段注入限流器，受保护配置控制是否启用及动作类型
- 配置同步通过 Redis Pub/Sub 实现跨节点一致性

```mermaid
graph LR
RL_LOCAL["本地限流器<br/>ratelimit.go"] --> PH["请求速率限制阶段<br/>phases.go"]
RL_REDIS["Redis 限流器<br/>redis.go"] --> PH
DROP_EXEC["阻断执行器<br/>drop.go"] --> DROP_REPO["阻断仓库<br/>drop_event.go"]
DROP_HANDLER["阻断处理器<br/>handler_drop.go"] --> DROP_EXEC
PH --> ENG["引擎<br/>engine.go"]
ENG --> APP["应用启动<br/>server.go"]
APP --> CFG["保护配置模型<br/>protection.go"]
APP --> PUBSUB["配置同步<br/>pubsub.go"]
RL_REDIS --> KV["Redis KV 缓存<br/>redis_kv.go"]
DROP_HANDLER --> ROUTER["路由注册<br/>router.go"]
```

**图表来源**
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)
- [redis.go:12-20](file://internal/waf/ratelimit/redis.go#L12-L20)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [handler_drop.go:31-98](file://internal/admin/handler_drop.go#L31-L98)
- [phases.go:103-128](file://internal/core/rules/phases.go#L103-L128)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)
- [server.go:90-125](file://internal/app/server.go#L90-L125)
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)
- [pubsub.go:13-31](file://internal/core/redis/pubsub.go#L13-L31)
- [redis_kv.go:13-21](file://internal/cache/redis_kv.go#L13-L21)
- [router.go:131-135](file://internal/admin/router.go#L131-L135)

**章节来源**
- [ratelimit.go:9-22](file://internal/waf/ratelimit/ratelimit.go#L9-L22)
- [redis.go:12-20](file://internal/waf/ratelimit/redis.go#L12-L20)
- [drop.go:19-47](file://internal/waf/drop/drop.go#L19-L47)
- [handler_drop.go:31-98](file://internal/admin/handler_drop.go#L31-L98)
- [phases.go:103-128](file://internal/core/rules/phases.go#L103-L128)
- [engine.go:103-106](file://internal/core/engine/engine.go#L103-L106)
- [server.go:90-125](file://internal/app/server.go#L90-L125)
- [protection.go:247-294](file://internal/store/protection.go#L247-L294)
- [pubsub.go:13-31](file://internal/core/redis/pubsub.go#L13-L31)
- [redis_kv.go:13-21](file://internal/cache/redis_kv.go#L13-L21)
- [router.go:131-135](file://internal/admin/router.go#L131-L135)

## 性能考量
- Redis 原子脚本：Lua 脚本减少往返次数，保证清理、计数、写入的原子性
- 过期与清理：本地限流器后台清理过期窗口，避免无限增长；Redis 限流器通过有序集合与 EXPIRE 自动回收
- 失败开策略：Redis 调用异常时允许请求，避免因外部依赖故障导致雪崩
- 阻断性能优化：阻断执行器使用原子计数器，避免锁竞争；连接关闭操作轻量级
- 热点规避：Redis 键包含"前缀:rl:键"，可通过前缀区分环境/租户；建议结合限流键的散列策略或分片键（在上游规则中扩展）
- 内存与 CPU：固定窗口对 CPU 友好；滑动窗口在高 QPS 下需关注有序集合大小与过期清理成本
- 监控：利用数据面指标统计请求总量、状态码分布、WAF 拦截数，辅助评估限流效果

**章节来源**
- [redis.go:67-85](file://internal/waf/ratelimit/redis.go#L67-L85)
- [ratelimit.go:98-116](file://internal/waf/ratelimit/ratelimit.go#L98-L116)
- [drop.go:85-100](file://internal/waf/drop/drop.go#L85-L100)
- [metrics.go:9-66](file://internal/dataplane/metrics.go#L9-L66)

## 故障排查指南
- 本地限流器不生效
  - 检查 Enabled 状态与 Reconfigure 是否正确调用
  - 确认键构造是否包含 Host，避免跨站点误判
- Redis 限流器异常
  - 查看 Redis 调用超时与错误日志
  - 确认 Lua 脚本执行结果与阈值比较逻辑
  - 失败开策略会放行请求，需结合监控定位
- 阻断策略问题
  - 检查阻断策略配置是否正确加载
  - 确认阻断统计是否正常更新
  - 验证阻断事件是否正确持久化到数据库
- 热重载未生效
  - 检查 Redis Pub/Sub 通道是否正常发布/订阅
  - 确认回调函数中是否正确调用 Reconfigure
- 前端策略未生效
  - 对比前端显示的窗口/配额与后端默认值
  - 确认保护配置已保存并触发热重载

**章节来源**
- [ratelimit_test.go:7-43](file://internal/waf/ratelimit/ratelimit_test.go#L7-L43)
- [server.go:220-242](file://internal/app/server.go#L220-L242)
- [pubsub.go:33-67](file://internal/core/redis/pubsub.go#L33-L67)
- [page.tsx:246-295](file://frontend/app/(dashboard)/cc-protection/page.tsx#L246-L295)
- [handler_drop.go:31-98](file://internal/admin/handler_drop.go#L31-L98)

## 结论
本项目提供了两种速率限制实现：本地固定窗口与 Redis 滑动窗口。前者简单高效，后者具备分布式一致性与更强的准确性。通过保护配置模型与热重载机制，系统实现了灵活的策略管理与跨节点同步。新增的阻断策略集成为系统提供了更强大的安全防护能力，支持基于自动阻断阈值的智能阻断和完整的统计监控。建议在高并发场景优先采用 Redis 限流器，并结合监控与键空间设计避免热点与资源浪费。

## 附录

### 速率限制算法对比
- 固定窗口（本地）
  - 优点：实现简单、CPU 开销低
  - 缺点：边界突发、无法平滑处理瞬时峰值
  - 适用：单节点、对精度要求不高
- 滑动窗口（Redis）
  - 优点：更贴近真实速率、抗边界突发
  - 缺点：有序集合增长与过期清理成本较高
  - 适用：多节点、强一致需求

### 键空间设计与一致性
- 本地：map[string]*window，键为"IP|Host"
- Redis：以"前缀:rl:键"命名，Lua 原子脚本保证清理、计数、写入的一致性
- 一致性：通过 Redis Pub/Sub 实现配置热重载的一致性

### 阻断策略配置参数
- **enabled**：全局阻断开关
- **bot_score_threshold**：Bot 分数阻断阈值（0-100）
- **cve_auto_drop_critical**：CVE Critical 级别自动阻断
- **cve_auto_drop_high**：CVE High 级别自动阻断

**章节来源**
- [ratelimit.go:19-22](file://internal/waf/ratelimit/ratelimit.go#L19-L22)
- [redis.go:74](file://internal/waf/ratelimit/redis.go#L74)
- [server.go:220-242](file://internal/app/server.go#L220-L242)
- [handler_drop.go:15-29](file://internal/admin/handler_drop.go#L15-L29)
- [protection.go:272-277](file://internal/store/protection.go#L272-L277)