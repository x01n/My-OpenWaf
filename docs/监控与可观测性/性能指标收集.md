# 性能指标收集

> [返回 监控与可观测性](监控与可观测性.md)

<cite>

<cite>
**本文档引用的文件**
- [internal/observability/metrics.go](file://internal/observability/metrics.go)
- [internal/dataplane/metrics.go](file://internal/dataplane/metrics.go)
- [internal/app/server.go](file://internal/app/server.go)
- [internal/observability/dropevent_writer.go](file://internal/observability/dropevent_writer.go)
- [internal/observability/botscore_writer.go](file://internal/observability/botscore_writer.go)
- [internal/observability/unified_writer.go](file://internal/observability/unified_writer.go)
- [internal/observability/writecoordinator.go](file://internal/observability/writecoordinator.go)
- [internal/store/repository/drop_event.go](file://internal/store/repository/drop_event.go)
- [internal/store/repository/bot_score.go](file://internal/store/repository/bot_score.go)
- [internal/store/repository/security_event.go](file://internal/store/repository/security_event.go)
- [docs/监控与可观测性/性能指标收集.md](file://docs/监控与可观测性/性能指标收集.md)
- [docs/扩展与插件/第三方集成/监控系统集成.md](file://docs/扩展与插件/第三方集成/监控系统集成.md)
- [frontend/app/(dashboard)/dashboard/page.tsx](file://frontend/app/(dashboard)/dashboard/page.tsx)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考量](#性能考量)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介
本文件面向性能指标收集系统，系统性梳理了 OpenWAF 的指标体系与实现，涵盖：
- 内置指标定义与用途：请求数量、阻断数量、观察检测、缓存命中率、上游错误等
- Prometheus 指标暴露机制：指标注册、数据格式与采集协议
- 运行时监控：goroutine 数量、堆内存分配、GC 暂停时间等
- 上游代理错误统计：连接失败、超时、响应异常的分类统计
- 指标查询与分析：常用查询语句、趋势分析与性能基线设定
- 可视化与告警：Grafana 仪表板设计与告警规则配置建议
- 新增指标：阻断事件来源统计、机器人检测评分、CVE 漏洞检测统计

## 项目结构
OpenWAF 将指标分为两类：
- 控制面（Admin）指标：通过 Prometheus 兼容的 `/metrics` 暴露，包含进程级运行时指标与 WAF 行为计数
- 数据面（Data Plane）指标：在请求处理链路中实时统计，提供 QPS、状态码分布、WAF 命中等
- 观测性事件指标：阻断事件、机器人评分、安全事件等异步写入与统计

```mermaid
graph TB
subgraph "控制面"
Admin["管理端 Hertz 服务"]
Prom["Prometheus 处理器<br/>/metrics"]
OM["观测性指标<br/>observability.Metrics"]
end
subgraph "数据面"
DP["数据监听器 Hertz 服务"]
MW["中间件处理器<br/>dataplane.Handler"]
DM["数据面指标<br/>dataplane.Metrics"]
Proxy["上游代理<br/>ForwardHTTP"]
Cache["响应缓存<br/>ResponseCache"]
end
subgraph "观测性事件"
UE["统一写入器<br/>UnifiedWriter"]
DEW["阻断事件写入器<br/>DropEventWriter"]
BSW["机器人评分写入器<br/>BotScoreWriter"]
end
Admin --> Prom
Prom --> OM
DP --> MW
MW --> DM
MW --> Proxy
MW --> Cache
MW --> UE
UE --> DEW
UE --> BSW
```

**图表来源**
- [internal/app/server.go:356](file://internal/app/server.go#L356)
- [internal/observability/metrics.go:52](file://internal/observability/metrics.go#L52)
- [internal/dataplane/metrics.go:35](file://internal/dataplane/metrics.go#L35)
- [internal/observability/unified_writer.go:37](file://internal/observability/unified_writer.go#L37)

**章节来源**
- [docs/监控与可观测性/性能指标收集.md:40-76](file://docs/监控与可观测性/性能指标收集.md#L40-L76)

## 核心组件
- 控制面指标（Prometheus）
  - 指标类型：计数器（counter）与仪表（gauge）
  - 关键指标：总请求数、阻断总数、观察检测总数、内置规则命中、缓存命中/未命中、上游错误、进程运行时（goroutines、heap alloc、sys、GC pause）、进程启动至今秒数
  - 暴露方式：Hertz 路由 `/metrics`，返回 Prometheus 文本格式
- 数据面指标
  - 指标类型：计数器与滑动窗口（环形缓冲）
  - 关键指标：总请求数、2xx/4xx/5xx 状态码分布、WAF 阻断/观察、内置规则命中、唯一客户端 IP、攻击源 IP、近 1/5 秒 QPS
- 观测性事件指标
  - 阻断事件来源统计：机器人、CVE、规则、IP信誉
  - 机器人检测评分：24小时总评分、阻断数量、高风险数量
  - 安全事件统计：类别分布、Top IP、Top 路径、时间线
- 缓存与上游代理
  - 响应缓存命中/未命中统计接入控制面指标
  - 上游代理错误统一计入控制面指标

**章节来源**
- [internal/observability/metrics.go:14-23](file://internal/observability/metrics.go#L14-L23)
- [internal/observability/metrics.go:52-125](file://internal/observability/metrics.go#L52-L125)
- [internal/dataplane/metrics.go:10-28](file://internal/dataplane/metrics.go#L10-L28)
- [internal/dataplane/metrics.go:83-99](file://internal/dataplane/metrics.go#L83-L99)
- [internal/store/repository/drop_event.go:68-102](file://internal/store/repository/drop_event.go#L68-L102)
- [internal/store/repository/bot_score.go:54-70](file://internal/store/repository/bot_score.go#L54-L70)

## 架构总览
控制面与数据面指标协同工作：
- 控制面负责进程级运行时指标与 WAF 行为统计
- 数据面负责请求级统计与实时 QPS 计算
- 上游代理错误与缓存命中统计在数据面处理后回传到控制面
- 观测性事件通过异步写入器批量写入数据库，避免阻塞数据面热路径

```mermaid
sequenceDiagram
participant C as "客户端"
participant DP as "数据监听器"
participant H as "处理器"
participant E as "WAF 引擎"
participant P as "上游代理"
participant M as "控制面指标"
C->>DP : "HTTP 请求"
DP->>H : "进入中间件"
H->>E : "执行 WAF 判定"
alt "拦截"
H->>M : "RecordBlock/RecordBuiltinHit"
H-->>C : "阻断响应"
else "转发"
H->>P : "ForwardHTTP"
P-->>H : "上游响应或错误"
H->>M : "RecordStatus/RecordUpstreamError"
H-->>C : "转发响应"
end
```

**图表来源**
- [internal/dataplane/metrics.go:39-68](file://internal/dataplane/metrics.go#L39-L68)
- [internal/observability/metrics.go:30-49](file://internal/observability/metrics.go#L30-L49)

## 详细组件分析

### 控制面指标（Prometheus）
- 指标定义与用途
  - openwaf_requests_total：累计处理的 HTTP 请求数
  - openwaf_blocks_total：累计阻断请求数
  - openwaf_observes_total：累计观察检测次数
  - openwaf_builtin_hits_total：累计内置 OWASP 规则命中
  - openwaf_cache_hits_total/openwaf_cache_misses_total：响应缓存命中/未命中
  - openwaf_upstream_errors_total：上游代理错误计数
  - openwaf_uptime_seconds：进程启动至今秒数（gauge）
  - openwaf_goroutines：当前 goroutine 数量（gauge）
  - openwaf_memory_alloc_bytes/openwaf_memory_sys_bytes：堆内存分配/从 OS 获取的总内存（gauge）
  - openwaf_gc_pause_total_ns：GC 暂停总时间（counter）
- 暴露机制
  - 注册路由：Admin 服务挂载 `/metrics`，调用 PrometheusHandler
  - 数据格式：Prometheus 文本格式，包含 HELP/TYPE 注释与指标值
  - 采集协议：标准 HTTP GET /metrics，无需认证
- 运行时指标来源
  - 通过 Go runtime 包读取 goroutine 数、内存分配、GC 暂停等

```mermaid
classDiagram
class Metrics {
+RequestsTotal : atomic.Int64
+BlocksTotal : atomic.Int64
+ObservesTotal : atomic.Int64
+BuiltinHits : atomic.Int64
+CacheHits : atomic.Int64
+CacheMisses : atomic.Int64
+UpstreamErrors : atomic.Int64
+Uptime : time.Time
+RecordRequest()
+RecordBlock()
+RecordObserve()
+RecordBuiltin()
+RecordCacheHit()
+RecordCacheMiss()
+RecordUpstreamError()
}
```

**图表来源**
- [internal/observability/metrics.go:14-49](file://internal/observability/metrics.go#L14-L49)

**章节来源**
- [internal/observability/metrics.go:52-125](file://internal/observability/metrics.go#L52-L125)
- [internal/app/server.go:356](file://internal/app/server.go#L356)

### 数据面指标（实时统计）
- 指标定义与用途
  - RequestsTotal：累计请求数
  - Status2xx/Status4xx/Status5xx：上游响应状态码分布
  - WAFBlocks/WAFObserves/BuiltinHits：WAF 阻断/观察/内置规则命中
  - UniqueIPs/AttackIPs：唯一访问 IP 与攻击源 IP 数
  - QPS1s/QPS5s：近 1/5 秒平均 QPS（基于 10×1s 环形桶）
- 统计逻辑
  - QPS：遍历最近 N 秒桶内计数求和并除以窗口长度
  - 唯一 IP/攻击 IP：使用并发安全 map，首次出现时计数加一
- 暴露方式
  - 提供 Summary 结构体用于 API 返回，前端可直接消费

```mermaid
flowchart TD
Start(["请求到达"]) --> Inc["RecordRequest()<br/>原子自增"]
Inc --> Classify{"状态码范围？"}
Classify --> |2xx| Stat2xx["RecordStatus(2xx)"]
Classify --> |4xx| Stat4xx["RecordStatus(4xx)"]
Classify --> |5xx| Stat5xx["RecordStatus(5xx)"]
Stat2xx --> End(["结束"])
Stat4xx --> End
Stat5xx --> End
```

**图表来源**
- [internal/dataplane/metrics.go:41-65](file://internal/dataplane/metrics.go#L41-L65)

**章节来源**
- [internal/dataplane/metrics.go:10-28](file://internal/dataplane/metrics.go#L10-L28)
- [internal/dataplane/metrics.go:83-135](file://internal/dataplane/metrics.go#L83-L135)

### 上游代理错误统计
- 错误来源
  - 上游连接失败、超时、响应异常等
- 统计位置
  - 数据面处理器在转发失败时设置 502 并记录上游错误
  - 控制面指标中的 UpstreamErrors 用于汇总
- 传输层优化
  - 共享 http.Transport，按 TLS 配置复用连接，降低连接开销

```mermaid
sequenceDiagram
participant H as "处理器"
participant P as "上游代理"
participant M as "控制面指标"
H->>P : "ForwardHTTP"
alt "发生错误"
P-->>H : "error"
H->>M : "RecordUpstreamError()"
H-->>H : "返回 502"
else "成功"
P-->>H : "响应"
H->>M : "RecordStatus(status)"
end
```

**图表来源**
- [internal/dataplane/metrics.go:39-68](file://internal/dataplane/metrics.go#L39-L68)
- [internal/observability/metrics.go:48](file://internal/observability/metrics.go#L48)

**章节来源**
- [internal/observability/metrics.go:48](file://internal/observability/metrics.go#L48)

### 缓存命中率与运行时指标
- 缓存命中率
  - 响应缓存命中/未命中分别记录到控制面指标
  - 命中率 = cache_hits / (cache_hits + cache_misses)
- 运行时指标
  - goroutines：当前 goroutine 数
  - memory_alloc_bytes：堆内存分配字节
  - memory_sys_bytes：从操作系统获取的总内存字节
  - gc_pause_total_ns：GC 暂停总纳秒数（counter）

**章节来源**
- [internal/observability/metrics.go:91-105](file://internal/observability/metrics.go#L91-L105)

### 阻断事件指标
- 指标定义与用途
  - 24小时阻断总数：Total24h
  - 来源分类统计：ByBot（机器人）、ByCVE（CVE）、ByRule（规则）、ByIPReputation（IP信誉）
- 统计逻辑
  - 基于 DropEvent 表的创建时间过滤最近24小时
  - 按 source 字段分类统计
- 暴露方式
  - 通过 DropEventRepo.Stats24h() 提供聚合统计
  - 支持按站点过滤 Stats24hBySite()

```mermaid
classDiagram
class DropEventRepo {
+Stats24h() DropStatsSummary
+Stats24hBySite(siteID) DropStatsSummary
+DeleteOlderThan(time) int64
}
class DropStatsSummary {
+Total24h : int64
+ByBot : int64
+ByCVE : int64
+ByRule : int64
+ByIPReputation : int64
}
DropEventRepo --> DropStatsSummary : "返回"
```

**图表来源**
- [internal/store/repository/drop_event.go:68-102](file://internal/store/repository/drop_event.go#L68-L102)

**章节来源**
- [internal/store/repository/drop_event.go:68-102](file://internal/store/repository/drop_event.go#L68-L102)

### 机器人检测指标
- 指标定义与用途
  - 24小时总检测数：Total24h
  - 24小时阻断数量：Blocked24h（action IN ('block','drop')）
  - 24小时高风险数量：HighRisk24h（is_high_risk = true）
- 统计逻辑
  - 基于 BotScoreLog 表的创建时间过滤最近24小时
  - 按 action 和 is_high_risk 字段条件统计
- 暴露方式
  - 通过 BotScoreRepo.Stats24h() 提供聚合统计

```mermaid
classDiagram
class BotScoreRepo {
+Stats24h() BotScoreStats
}
class BotScoreStats {
+Total24h : int64
+Blocked24h : int64
+HighRisk24h : int64
}
BotScoreRepo --> BotScoreStats : "返回"
```

**图表来源**
- [internal/store/repository/bot_score.go:54-70](file://internal/store/repository/bot_score.go#L54-L70)

**章节来源**
- [internal/store/repository/bot_score.go:54-70](file://internal/store/repository/bot_score.go#L54-L70)

### CVE 检测指标
- 指标定义与用途
  - CVE 漏洞检测统计：通过 CVEDetector 的检测结果聚合
  - 支持按敏感度级别（none/off）跳过子检测器
- 统计逻辑
  - Detect 方法按类别顺序执行检测器
  - 支持 per-category 敏感度覆盖
- 暴露方式
  - 通过安全事件统计接口提供类别分布、Top IP、Top 路径等

**章节来源**
- [internal/waf/cve/detector.go:214-241](file://internal/waf/cve/detector.go#L214-L241)
- [internal/store/repository/security_event.go:94-121](file://internal/store/repository/security_event.go#L94-L121)

### 观测性事件写入与统计
- 统一写入器（UnifiedWriter）
  - 接收安全事件、访问日志、阻断事件、机器人评分四类记录
  - 使用大缓冲通道（16384/8192）避免阻塞热路径
  - 固定间隔批量写入数据库，单事务保证一致性
- 阻断事件写入器（DropEventWriter）
  - 支持 Redis 双写，推送至 openwaf:drop_events 列表
  - 支持 WriteCoordinator 序列化 DB 写入
  - 缓冲区满时非阻塞丢弃，避免内存膨胀
- 机器人评分写入器（BotScoreWriter）
  - 支持 Redis 双写，推送至 openwaf:bot_scores 列表
  - 支持 WriteCoordinator 序列化 DB 写入
- 归档与清理
  - DropEventRepo.DeleteOlderThan() 定期清理过期事件
  - Redis 列表设置 TTL（7天），并限制最大长度

```mermaid
sequenceDiagram
participant H as "处理器"
participant UW as "统一写入器"
participant DEW as "阻断事件写入器"
participant BSW as "机器人评分写入器"
participant WC as "写入协调器"
participant DB as "数据库"
participant RD as "Redis"
H->>UW : "RecordDropEvent/RecordBotScore"
UW->>DEW : "阻断事件"
UW->>BSW : "机器人评分"
DEW->>RD : "LPUSH openwaf : drop_events"
DEW->>WC : "Submit(BatchCreate)"
BSW->>RD : "LPUSH openwaf : bot_scores"
BSW->>WC : "Submit(BatchCreate)"
WC->>DB : "Transaction(CreateInBatches)"
```

**图表来源**
- [internal/observability/unified_writer.go:59-89](file://internal/observability/unified_writer.go#L59-L89)
- [internal/observability/dropevent_writer.go:115-134](file://internal/observability/dropevent_writer.go#L115-L134)
- [internal/observability/botscore_writer.go:115-134](file://internal/observability/botscore_writer.go#L115-L134)

**章节来源**
- [internal/observability/unified_writer.go:16-52](file://internal/observability/unified_writer.go#L16-L52)
- [internal/observability/dropevent_writer.go:19-55](file://internal/observability/dropevent_writer.go#L19-L55)
- [internal/observability/botscore_writer.go:19-55](file://internal/observability/botscore_writer.go#L19-L55)
- [internal/observability/writecoordinator.go:11-39](file://internal/observability/writecoordinator.go#L11-L39)

## 依赖关系分析
- 控制面与数据面解耦：控制面指标独立于数据面处理链，仅在必要处记录
- 指标注册点集中：Admin 服务统一注册 `/metrics` 路由
- 指标来源分散但聚合：数据面统计请求级指标，控制面统计进程级与行为指标
- 观测性事件异步化：统一写入器将多类事件批量写入，避免阻塞热路径

```mermaid
graph LR
App["应用入口<br/>Run()"] --> Admin["Admin 服务"]
App --> Data["数据监听器"]
Admin --> Prom["PrometheusHandler"]
Data --> Handler["dataplane.Handler"]
Handler --> MetricsDP["dataplane.Metrics"]
Handler --> Proxy["proxy.ForwardHTTP"]
Handler --> MetricsCtrl["observability.Metrics"]
Handler --> UE["UnifiedWriter"]
UE --> DEW["DropEventWriter"]
UE --> BSW["BotScoreWriter"]
Proxy --> MetricsCtrl
```

**图表来源**
- [internal/app/server.go:356](file://internal/app/server.go#L356)
- [internal/dataplane/metrics.go:35](file://internal/dataplane/metrics.go#L35)
- [internal/observability/metrics.go:52](file://internal/observability/metrics.go#L52)

**章节来源**
- [internal/app/server.go:356](file://internal/app/server.go#L356)

## 性能考量
- 指标更新的原子性与低开销：使用原子计数器与并发安全容器，避免锁竞争
- 环形缓冲计算 QPS：固定大小桶数组，O(N) 扫描窗口，时间复杂度稳定
- 缓存命中与清理：分片哈希减少锁争用；后台定时清理过期条目
- 上游传输复用：按 TLS 配置缓存 http.Transport，显著降低连接建立成本
- 异步事件写入：缓冲队列与批量写入，避免阻塞请求处理
- 写入协调：单 goroutine 序列化 DB 写入，避免 SQLite 锁争用

## 故障排查指南
- 指标不更新
  - 检查 Admin 服务是否正确注册 `/metrics` 路由
  - 确认 Prometheus 抓取目标可达且未被防火墙阻断
- 上游错误频繁
  - 查看 UpstreamErrors 是否持续增长
  - 检查上游地址配置、TLS 设置与网络连通性
- QPS 异常波动
  - 对比数据面 Summary 中的 QPS1s/QPS5s 与外部抓取结果
  - 关注 UniqueIPs/AttackIPs 是否异常升高
- 缓存命中率低
  - 检查缓存 TTL、最大容量与请求方法是否为 GET
  - 对热点资源确认缓存键生成一致性
- 观测性事件丢失
  - 检查写入器缓冲区是否溢出（非阻塞丢弃）
  - 确认 Redis 连接与管道执行状态
  - 验证数据库事务执行情况

**章节来源**
- [internal/app/server.go:356](file://internal/app/server.go#L356)
- [internal/observability/unified_writer.go:113-155](file://internal/observability/unified_writer.go#L113-L155)

## 结论
OpenWAF 的指标体系覆盖控制面与数据面，既满足 Prometheus 采集需求，又能在高并发场景下保持低开销与高可用。通过清晰的指标定义与合理的统计口径，用户可以快速定位性能瓶颈、评估 WAF 效果并建立稳定的监控与告警体系。新增的阻断事件、机器人检测和 CVE 检测指标进一步丰富了安全运营的可视化能力。

## 附录

### 指标查询与分析指南
- 常用查询语句
  - QPS 近 1 分钟：`rate(openwaf_requests_total[1m])`
  - 阻断率：`sum(rate(openwaf_blocks_total[5m])) / sum(rate(openwaf_requests_total[5m]))`
  - 缓存命中率：`rate(openwaf_cache_hits_total[5m]) / (rate(openwaf_cache_hits_total[5m])+rate(openwaf_cache_misses_total[5m]))`
  - 上游错误率：`sum(rate(openwaf_upstream_errors_total[5m])) / sum(rate(openwaf_requests_total[5m]))`
  - 运行时资源：`go_goroutines`、`go_memstats_alloc_bytes`、`go_gc_duration_seconds`
- 趋势分析
  - 对比近 7 天/30 天的 QPS、阻断数与错误率变化
  - 关注特定时间段的峰值与异常波动
- 性能基线设定
  - 基于历史均值与分位数设定告警阈值
  - 结合业务高峰时段调整窗口与阈值

### 指标可视化配置（Grafana）
- 仪表板建议
  - 实时 QPS 曲线（数据面 Summary）
  - 阻断与观察趋势（控制面指标）
  - 缓存命中率与上游错误率
  - 运行时资源（goroutines、内存、GC）
  - 阻断事件来源分布（机器人/CVE/规则/IP信誉）
  - 机器人检测评分统计
- 告警规则示例
  - QPS 突增/突降
  - 阻断率超过阈值
  - 上游错误率异常
  - 缓存命中率骤降
  - goroutine 数过高或内存增长过快
  - 阻断事件来源异常波动

**章节来源**
- [frontend/app/(dashboard)/dashboard/page.tsx:55-254](file://frontend/app/(dashboard)/dashboard/page.tsx#L55-L254)